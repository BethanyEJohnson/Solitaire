public class Cache {
	//one of four cache slots at the top
	//could be a stack (last in, first out structure)
	//card can be dragged and dropped into it's vicinity and dropped if cache behavior requirements are met
	//initializes as an empty priority queue
}
